import { IPost } from '../../types/app-types';

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const addNewPost = (info: IPost, image: any) => {
  console.log(info);
  console.log(image);
};
